/** Add opt-in timed-shot planning; existing episodes remain unchanged. */
import { makeSeedMigrations } from './_seedStageHelpers.js';
export default makeSeedMigrations(['fableloom-plan-shots', 'fableloom-review-shots']);
